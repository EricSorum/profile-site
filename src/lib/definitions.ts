export type MenuLinkType = {
  id: number,
  txt: string,
  url: string,
  right: boolean
}

export type FeatureType = {
  id: number,
  title: string,
  txt: string,
  img: string,
  alt: string
}

export type PageType = {
  id?: number,
  title?: string,
  txt?: string,
  img?: string,
  alt?: string
}

export type CardType = {
  id: number,
  title: string,
  txt: string
}